{
  "description": "App-specific TypeScript overrides — resolves @shooteger/nuxt-stripe to the built type declarations.",
  "extends": "./.nuxt/tsconfig.app.json",
  "compilerOptions": {
    "paths": {
      "@shooteger/nuxt-stripe": ["../dist/types.d.mts"]
    }
  }
}
